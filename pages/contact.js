import React from "react";
import Menu from "../components/Menu";

export default function contact() {
    return (
        <div>
            <Menu />
            <h1 style={{ textAlign: "center" }}>We are in the contacts page</h1>
        </div>
    );
}
